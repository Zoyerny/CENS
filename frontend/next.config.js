/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost'], // Ajoutez ici les noms d'hôte supplémentaires si nécessaire
  },
};

module.exports = nextConfig;
